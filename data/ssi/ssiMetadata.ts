import { IEnvMetadata } from "@/data/types";

export const envMetadata : IEnvMetadata = {
    name: "Salt Spring Island",
    logo: "/svg/Globe.svg",
    includeSurf: true,
    includeTrails: true,
    includeFAQ: true,
}